#include "main.h"
/**
 * print_rev - take pointer and print string in reverse
 * @s: pointer of string
 */
void print_rev(char *s)
{
	int str_len = _strlen(char *s);
	char str[] = *s;

	for (i = str_len; i >= 0; i--)
		_putchar(str[i]);
	}
	_putchar('\n');
}
