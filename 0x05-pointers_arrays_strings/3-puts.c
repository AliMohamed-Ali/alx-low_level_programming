#include "main.h"
/**
 * _puts - take pointer and print string
 * @str: pointer of string
 */
void _puts(char *str)
{
	while (1)
	{
		if (*str == '\0')
		{
			_putchar('\n');
			break;
		_putchar(*str);
		str++;
	}
	return (i);
}
