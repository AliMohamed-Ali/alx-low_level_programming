#include "main.h"
/**
 * _abs - Entry point
 * @n: The character to check
 * Description: return absolute
 * Return: Always reurn int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
